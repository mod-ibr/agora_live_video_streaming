import 'package:flutter/material.dart';

class CustomBroadcastView extends StatelessWidget {
  const CustomBroadcastView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
